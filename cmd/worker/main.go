package main

// Entrypoint do processo Worker (Asynq).
// Responsabilidade: conectar ao Redis, registrar os handlers de tasks
// e iniciar o pool de até 50 goroutines consumindo a fila.
// Pode rodar como processo separado do API (deploy independente).

func main() {}
